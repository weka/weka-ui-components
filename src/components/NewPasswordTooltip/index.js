import React from 'react'
import NewPasswordTooltip from './NewPasswordTooltip'

const NewPasswordTooltipWrapper = (props) => <NewPasswordTooltip {...props} />

export default React.memo(NewPasswordTooltipWrapper)
