import React from 'react'
import CircularProgress from './CircularProgress'

const CircularProgressWrapper = (props) => <CircularProgress {...props} />

export default React.memo(CircularProgressWrapper)
