import React from 'react'
import MultiValueContainer from './MultiValue'

const MultiValueContainerWrapper = (props) => <MultiValueContainer {...props} />

export default React.memo(MultiValueContainerWrapper)
