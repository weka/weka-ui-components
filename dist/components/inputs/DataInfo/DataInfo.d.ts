import { default as React } from 'react';
interface DataInfoProps {
    label: string;
    value: string;
}
declare function DataInfo(props: DataInfoProps): React.JSX.Element;
export default DataInfo;
