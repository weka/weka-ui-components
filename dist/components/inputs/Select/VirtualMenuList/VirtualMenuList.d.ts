import { default as React, ReactNode } from 'react';
type VirtualMenuListProps = {
    children: ReactNode;
    maxHeight?: number;
};
declare const VirtualMenuList: React.FC<VirtualMenuListProps>;
export default VirtualMenuList;
